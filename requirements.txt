# Core
pydantic>=2.7,<3

# LLM SDK for the reasoning core (agent/)
# TODO: pin exact version when the LLM call is implemented
anthropic

# --- Text extraction (ingestion/) — choose ONE path during implementation ---
# TODO: decide based on what the scanned docs look like (see ingestion/CLAUDE.md)
# pypdf                  # if PDFs carry a text layer
# pdf2image              # if scanned images: PDF -> image ...
# pytesseract            # ... -> OCR (Greek language pack: 'ell')
# Pillow                 # image handling for the OCR path

# Dev
pytest
