# Core
pydantic>=2.7,<3

# LLM SDK for the reasoning core (agent/)
# TODO: pin exact version when the LLM call is implemented
anthropic

# Text extraction (ingestion/) — v1: PDF text layer
pypdf>=6,<7
cryptography>=3.1  # pypdf needs it for AES-encrypted PDFs (empty-password docs are common)

# Dev
pytest
